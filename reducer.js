const init = {
    product: {
        name: "Nike Air",
        description: "Nike Air Force 1",
        price: 30000,
    },
};

export default function reducer(state = init, action, args) {
    switch (action) {
        default:
            return state;
    }
}
